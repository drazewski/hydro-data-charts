# Copy this file to .env and fill in your local credentials
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=db_hydro
DB_PORT=3306
DB_DIALECT=mysql

# Frontend: base URL for API (no trailing /api)
NEXT_PUBLIC_VITE_BASE_URL=http://localhost:8080
